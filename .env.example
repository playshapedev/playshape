# Playshape — Environment Variables
# Copy this file to .env and adjust as needed.
# No env vars are required for local development.

# Database path override (optional, defaults to ./data/ in dev, userData in production)
# PLAYSHAPE_USER_DATA=/path/to/data

# Nuxt dev server port (optional, defaults to 3200)
# NUXT_PORT=3200
